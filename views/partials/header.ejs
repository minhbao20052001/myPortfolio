<header class="header">
    <nav class="nav nav__portfolio">
        <% if(locals.locals_user) {%>
            <a class="nav__el" href="/get_me">My Portfolio</a>
            <% }else{ %>
                <a class="nav__el" href="/">My Portfolio</a>
                <%} %>

                    <form class="nav__search"><input class="nav__search-input" type="text"
                            placeholder="Search User" /><button class="nav__search-btn"><i
                                class="fas fa-search"></i></button></form>
    </nav>
    <nav class="nav nav--user">
        <% if(locals.locals_user) {%>
            <a class="nav__el nav__el--logout">Logout</a>
            <a class="nav__el" href="/message">Message online</a>
            <a href="/set_user" class="nav__el">
                <img src="/img/<%= locals.locals_user.photo %>" alt="user-1" class="nav__user-img">
                <span id="id-user-current" data-id="<%= locals.locals_user._id %>">
                    <%=locals.locals_user.name%>
                </span>
            </a>
            <% }else{ %>
                <a class="nav__el" href="/login">Log in</a><a class="nav__el nav__el--cta" href="/signup">Sign
                    up</a>
                <%} %>
    </nav>
</header>